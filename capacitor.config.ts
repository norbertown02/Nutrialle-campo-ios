import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.nutrialle.campo',
  appName: 'Nutrialle Campo',
  webDir: 'dist',
};

export default config;
